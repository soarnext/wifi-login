# 更新日志

本文件记录各版本变更。发版说明由 CI 从本文件按版本号自动抽取，见 [Releases](../../releases)。

---

## [1.1.0] - 2026-09-13

### 新增

- **认证框架化**: 本体只提供通用框架 (连通性检测、认证页类型自动识别、适配器注册表、UI、
  账号存储、日志), 认证协议由**可插拔适配器模块**实现 (`ui/src/services/portal-adapters/`),
  内置 Panabit 适配器作为默认适配器与演示模板
- **认证页类型自动识别**: 检测到被强制门户拦截后, 各适配器按页面特征 (跳转 URL 参数 /
  页面标题 / 内容 / 响应头) 评分取最高; 识别不出回退该 WiFi 上次成功登录所用的适配器,
  再不行界面「认证类型」选择行由用户手动选择 (选完可手动输入服务器地址);
  登录成功按 WiFi 记住所用适配器, 下次同 WiFi 优先复用
- **适配器能力化**: 设备管理入口按适配器能力显示 (提供 `listDevices` 才显示);
  外部调用支持 `adapter=<id>` 强制指定适配器
- **适配器开发指南**: 仓库根目录 `ADAPTER.md` (接口契约 + 全流程) 与配套 `SKILL.md`
  (前端代码来源询问 / 设备加密能力探查 / 阻塞情形告知 / 问题主动报告)

### 优化

- **界面美化**: 四页统一设计语言 (状态色点、分隔线、消息色条、顶栏对齐、Wi 徽标、
  日志按 tag 着色、版本徽章), 配色改为纯黑主题
- **渲染/解码性能**: 日志页内容未变跳过整列表重渲染; 管理页设备列表对比更新并以
  在线 IP 作列表 key (下线单台不整列重渲); base64 解码预建查找表 (每字符 O(1))

---

## [1.0.0] - 2026-09-12

首个正式版。运行在有道词典笔（960×266 横条屏）上的
Panabit 网页认证登录工具，用于替代笔上打不开的认证网页。

### 软件功能

**连通性检测**

- 使用小米 / vivo / 华为三个国内探测源，**并发竞速**：同时发起，首个给出确定结论的
  直接采纳（正常网络下约 35ms 出结果，而非逐个串行等待）；单源失败不判决，全部失败才判无网络
- 被强制门户劫持时，解析出跳转页面（支持 302 Location、meta refresh、location.href、
  带 `wlanuserip` / `paip` 参数的链接），展示 Portal 服务器地址与跳转 URL
- 网络直连正常时提示"无需登入"，已认证则自动进入设备管理页

**登录与凭据**

- Panabit `webauth/user_login` 账号密码登录，密码经 AES-128-ECB/ZeroPadding 加密
  （密钥 `Panabit@1024_key`，与网页端 `pa_aes_encode` 一致）
- **记住密码按 WiFi 隔离**：每个 WiFi 各存各的账号，换网络不会串用上一个网络的密码
  （存储结构 `{ accounts: { "<ssid>": {...} } }`，WiFi 名称由原生 `panet.wifiSsid()` 读取）
- **密码不显明文**：已记住的密码只显示圆点掩码；新输入的密码明文显示 3 秒便于核对
- 账号 / 密码通过 `global.startTextEdit` 唤起系统级"有道输入法"输入

**设备管理**

- 在线设备列表（`ucenter/load_user_list`）：设备名 / 在线 IP / MAC / 在线时长，自动标记「本机」
- 单设备下线（`ucenter/user_offone`）：每台设备一个下线按钮 + 自绘确认弹层
- 全部下线（`ucenter/user_offall`）：下线后自动返回主页重新检测
- 列表 15 秒自动刷新

**会话与稳定性**

- 会话保活：认证页停留期间 30 秒心跳，避免 token 过期；会话过期类失败自动刷新重试一次
  （密码错误 / 锁定 / 需改密不重试）
- 前后台切换不冲突：每次重检会取消上一次在途探测，迟到结果不再覆盖新状态；
  输入法面板引起的 onHide/onShow 被识别并抑制，不会冲掉登录表单
- 服务器不可达时行为收敛：管理页拿不到数据会自动退回主页，并进入 3 分钟冷却，
  不会在主页与管理页之间反复跳转
- 运行日志写入 `/userdisk/xiro/wifi.log`（自动建目录、超过 512KB 轮转），
  应用内「日志页」可直接查看最近 120 行
- 支持手动指定认证服务器（探测失败或无响应时）

**外部调用接口**

- 其他小程序可通过 `navTo('falcon://8001865309000001/index', {...})` 调用：
  `action=login|log|check`、预填 `server` / `username` / `password`、`auto=1` 自动提交
- `action=check` 的检测结果经事件回传给调用方，callback 名可指定

### 安装

```sh
adb push 8001865309000001.1_0_0.amr /data/local/tmp/
adb shell miniapp_cli install /data/local/tmp/8001865309000001.1_0_0.amr
adb shell miniapp_cli start 8001865309000001
```

### 设备兼容性

- **仅在有道词典笔 X6PRO 上完成真机验证**，其他机型未做适配测试
- 其他机型如遇到问题，欢迎提交 Issues（附机型、固件版本与 `wifi.log`）或提交 PR
- PR 合并前提：不得影响 X6PRO 上已验证的现有功能；
  机型差异请用能力探测做分支处理，不要直接改掉通用逻辑

### 已知限制

- 认证服务器不可达时管理页不可用，此时主页会提示并停止自动进入（3 分钟冷却）
- 设备 IP 无法从固件查询（jsapi 无 `nm` 模块），Portal 参数以服务器下发的跳转参数为准
- 服务端 GB2312 中文无法转 UTF-8（固件无 `misc` 模块），错误信息用错误码本地映射代替

[1.0.0]: https://github.com/soarnext/wifi-login/releases/tag/v1.0.0
