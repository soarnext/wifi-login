---
name: wifi-portal-adapter
description: 为 WiFi 认证小程序 (wifi-login 仓库) 制作新的 Portal 适配器模块, 适配新的 WiFi 登入页面/认证系统。覆盖从网站分析到模块注册的全流程: 询问前端代码获取来源 (本地文件/浏览器)、探查设备加密能力 (不支持则告知无法制作)、分析认证协议、识别阻塞情形 (代码加密/验证码/后端绑定验证)、实现与注册适配器、真机验证。当用户要适配新的认证页、新的 Portal、其他厂商认证系统时使用。
---

# WiFi Portal 适配器制作

为 wifi-login 小程序制作新的认证协议适配器。本体是**框架**（检测、自动识别、
注册表、UI、存储），具体认证协议由 `ui/src/services/portal-adapters/` 下的
**适配器模块**实现；内置 `panabit.js` 是默认适配器和演示模板。

## 必读上下文

- `ADAPTER.md`（仓库根目录）— 接口契约 + 开发全流程文档
- `SKILL.md`（仓库根目录）— 本 skill 文件
- `ui/src/services/portal-adapters/registry.js` — 注册表 + 自动识别逻辑
- `ui/src/services/portal-adapters/panabit.js` — 默认适配器（演示模板）
- `ui/src/services/aes.js` — 框架内置加密能力（当前仅 AES-128-ECB/ZeroPadding）
- `ui/src/services/detect.js` — 检测结果字段（适配器 match 的 ctx 来源）
- 设备画像与真机命令见相关设备 profile / 交接文档（960×266 横条屏, QuickJS 运行时）

## 核心流程（按顺序，不得跳步）

### 1. 询问前端代码获取来源（动手分析前必须先问，不要假设）

向用户确认认证页前端代码从哪里来，可用选项：

- **本地文件**：用户手上已有认证页 HTML/JS（抓包导出/另存/登入页目录），
  询问文件路径后直接读取分析。
- **浏览器**：在需要认证的网络下用浏览器（Browser Use）打开任意 HTTP 站点，
  被强制门户劫持到认证页后抓取 HTML/JS/登录请求。
- **设备侧**：adb 从词典笔拉取（设备无通用浏览器，能拿到的有限，只作补充）。
- **仅凭 URL**：用户只给认证服务器地址——说明这样拿不到前端代码，
  通常只能先做探测式分析并明确告知局限。

用户没说来源时，用一句话问清（哪种方式 / 文件在哪 / 认证页 URL），
再进入步骤 2。

**提取要求**：必须拿全**登录页与管理页的全部 HTML / CSS / JS**（含内联与外链、
iframe 子页、登录后动态加载的脚本）——加密实现、参数构造、返回码处理往往藏在
外链 js 里，漏一个文件就可能误判协议。各平台（Chrome/Edge、Firefox、手机浏览器、
curl/wget、mitmproxy/Fiddler/Charles）的具体提取步骤与完整性自查清单见
`ADAPTER.md` 步骤 1。

### 2. 探查加密能力（硬门禁：设备不支持就告知无法制作）

- 框架/设备的加密基线：仓库 `ui/src/services/aes.js` 当前只内置
  **AES-128-ECB/ZeroPadding 纯 JS**；设备是 QuickJS 运行时（无 Node/OpenSSL，
  大数与非对称运算性能不可行）。
- 从前端代码确认登录密码的真实加密方式（明文 / AES-hex / AES-base64 /
  MD5 / SHA / RSA / SM 系列 / 魔改算法），以及密钥来源
  （写死在 JS / 每次会话下发 / 前端计算派生）。
- 判定并告知：
  - AES-ECB 类（含其他 padding/编码）→ 可做；padding 与编码差异在适配器内处理。
  - 简单摘要（MD5/SHA1）→ 可做，但需新增纯 JS 摘要实现并自测后再交付。
  - **RSA / SM2/SM4 / 魔改算法 / 动态密钥交换且派生不可复现 → 明确告知用户
    无法制作**：说明原因（QuickJS 性能与实现成本）、证据（代码位置）、
    可选出路（native 扩展另行评估 / 换认证方式）。

### 3. 分析认证协议

从代码提取并记录（写成清单再动手）：

- 登录端点：URL / 方法 / 参数表；每个参数的来源，特别注意本机 IP/MAC/VLAN
  能否由框架检测参数（`wlanuserip`/`clientmac`/`vlan`）覆盖。
- 密码字段形态（加密后 hex/base64）与 remember 等开关。
- 返回码语义（成功/账密错误/锁定/需改密/会话过期）。
- 响应编码（多为 GB2312，按 ASCII 清洗后解析，文案本地映射）。
- 状态心跳端点（有 → 实现 `queryStat`）；下线/设备列表端点（有 → 实现
  `logout`/`listDevices`/`offOne`/`offAll`）。

### 4. 阻塞情形必须立即告知（不要猜、不要静默降级）

出现以下任一情况，**停下来**向用户报告现象、证据、影响与可选出路：

- **前端代码被加密/混淆到无法分析**（整个文件，或登录提交/加密关键路径）。
- **后端验证无法通过纯 API 登入**：登录依赖验证码、短信、滑块、设备指纹、
  Cookie+JS 挑战、后端绑定校验等无法用纯 HTTP 请求复现的机制。
- 加密方式超出步骤 2 的设备能力（硬门禁）。
- 协议存在多种解释且代码/抓包无法确认（带证据询问）。

告知后再由用户决定继续方向；绝不在未告知的情况下宣称适配器可用。

### 5. 实现适配器

- 复制 `panabit.js` 为 `<协议>.js`（保持接口形态），改协议细节。
- 必选：`match`（识别评分 0-100，用 URL/标题/内容的稳定特征加权）、
  `loadConf`（归一化状态 free/ready/manual/other/error）、
  `login`（会话刷新与失败重试在适配器内自理）。
- 可选：`queryStat`/`logout`/`sync`/`listDevices`/`offOne`/`offAll`
  （提供 listDevices 才会在界面出现"设备管理"入口）。
- 协议语义全部收口在适配器内，页面不出现协议分支；
  复用 `adapter-api.js` 的 `qs()`/`sanitize()`。

### 6. 注册与验证

- 在 `registry.js` 的 `ADAPTERS` 数组注册（选择列表与自动识别随之生效）。
- 本地：更新/运行 `test/` 注入桩测试（纯逻辑：match 评分、请求 URL 构造、
  归一化分支）；`pnpm -C ui build` 编译检查。
- 真机：安装 → 连目标认证网络 → 看日志 `[适配器] 自动识别 <id> score=<n>` →
  登录 → 复查放行；识别不出时走界面"认证类型"手选验证。
- **Panabit 回归**：新适配器不得影响 Panabit 已验证行为（Panabit 网络下
  自动识别得分必须仍最高，登录/设备管理不回归）。

## 问题报告（全程要求）

制作过程中发现的任何问题都要**主动、及时报告**，不要静默跳过或用猜测填空：
协议自相矛盾、代码缺失、端点不通、加密不可复现、构建/测试失败、
真机行为与预期不符——都说明现象、证据（文件/行/日志）、影响与建议。
